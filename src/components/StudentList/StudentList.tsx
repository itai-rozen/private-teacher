import React, {useState} from 'react'
import { getStudents, deleteStudent } from '../../firestore'
import './studentList.css'

const StudentList:React.FC = () => {
  return <div className="students-container">
    
  </div>
}

export default StudentList